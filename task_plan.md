# 4.1 产线单元智能控制——实施与验收

更新日期：2026-09-13。状态：用户已批准实施；M0–M4 已完成，真实 WebGL 浏览器流程与异常恢复验证通过。

## 目标与范围

用 Python 实现控制器，用 Unity 模拟皮带、传感器、气缸、分拣搬运机构及物料，通过用户指定的 EMQX 公共 MQTT 服务交换数据。总体覆盖 PDF 4.1，按四个模块逐步完成，首个可运行里程碑是皮带四状态机与对应仿真闭环。

本阶段包含：状态机控制、皮带输送、分拣搬运、物料检测与分拣逻辑。后续赛题的多关节机器人、AGV、赋码、EdgeX、MES、OEE 不纳入本阶段实现。

## 当前工作的步骤

- [x] 核实工作区和用户材料。
- [x] 用户已选择浏览器网页 Unity WebGL。
- [x] 核实参考库、WebGL 通信路线、资产格式和仓库许可证；具体依赖审计和模型导入留在实施阶段。
- [x] 比较受控运动反馈与全刚体物理，推荐前者。
- [x] 完成状态机、通信、设备模型、交付和验收讨论稿。
- [x] 自查设计矛盾、缺失参数和材料未覆盖之处；补齐普通复位与物料队列的一致性规则。
- [x] 完成面向用户的讨论稿，供本轮交付；下一轮由用户提出调整。
- [x] 用户确认实施，指定 GitHub 仓库；形成已确认设计和实施清单。
- [x] M0：准备环境、锁定第三方来源、导入代表性工业模型。
- [x] M1：实现并验证四状态、MQTT、WebGL 皮带闭环。
- [x] M2：反馈驱动输送、检测、分隔、挡停、顶升。
- [x] M3：平移、升降、夹爪搬运和分类联动。
- [x] M4：连续生产、异常验证、构建与操作说明。

已初始化 Git 并关联用户指定的空仓库。开发按里程碑提交，失败时保留日志并回退到最近有效检查点，避免覆盖远端历史。

## 实现阶段草案

1. 固定需求、I/O 数据契约、来源清单和本地工具版本。
2. 完成四状态机、控制面板、灯、皮带与 MQTT 通信的最小闭环。
3. 完成输送、分隔、挡停、顶升与检测工位的反馈驱动流程。
4. 完成平移气缸、升降与夹爪组成的分拣搬运流程，联调三种分拣结果。
5. 验收暂停恢复、急停清料、通信中断、重复或陈旧消息、初始化及连续物料处理。
6. 交付 Unity 工程、WebGL 构建、Python 控制器、测试、操作说明和第三方来源清单。

## 暂定设计原则

- Python 为正常业务控制唯一决策方；Unity 为设备位置、传感器、实际动作结果的事实来源。
- Unity 中按钮通过 MQTT 提交操作事件。急停和失联保护可立即在仿真本地停止动作，随后同步控制器。
- 保留四个业务 Status 值；初始化、通信故障及准备就绪使用独立标记，避免擅自增加竞赛状态。
- 暂停保存流程步骤和物料；急停清除所有虚拟物料并废弃旧指令；复位后不自动运行。
- 优先使用反馈条件推进步骤，定时器用于去抖、延时和超时判定，避免按固定时长播放整段动画。
- 优先复用有来源与许可记录的工业资产，新增工作主要为导入、装配、碰撞体和动作绑定。
- Stars 是筛选信号，不等于安全保证；具体版本、依赖兼容和资产授权必须单独核实。
- 用户已接受复用近似外观模型，重点对齐模块布局、信号和动作。
- 详细讨论稿见 docs/4.1-conveyor-webgl-plan.md，研究来源与具体仓库记录见 findings.md。

## 尚需讨论

- 平台已确认 WebGL；浏览器通过 MQTT over WSS，Python 使用 MQTT TCP 接入同一 broker。
- 完整 I/O 表和工序步骤未出现在截图中；缺失内容先列为建议定义，不能称为比赛原始规范。
- 视觉目标已获用户明确回复：接受近似外观、优先模型复用和控制闭环。

## 错误与限制

- 初次排查未发现 Unity；现已安装到 D 盘、完成激活和 WebGL 构建。
- 本机 Python 3.14 独立环境已完成兼容性和闭环测试。
- GitHub 匿名 API 触发速率限制；后续改用原始仓库页面、raw 内容和官方文档核实，不重复 API 请求。
