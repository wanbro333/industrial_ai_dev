# 验收记录

日期：2026-09-13。当前尚未完成整项 WebGL 浏览器验收。

| 检查 | 实际结果 |
|---|---|
| Python 状态机、时效/去重、C# 设备闭环 | 26 项通过（Python 3.14 + .NET 8）；覆盖四种分类、空载具、夹持暂停/急停和到位超时 |
| 浏览器协议规则（Node 运行） | 2 项通过，覆盖旧代次、重复、乱序、过期、retained 和单控制器绑定 |
| Unity C# 静态编译 | 基于本机 6000.3.24f1 的 Unity 程序集编译通过，0 警告/0 错误；不等同 Unity 项目构建 |
| 真实 EMQX 往返 | Paho TCP 1883 ↔ MQTT.js WSS 8084，5 次成功，357/341/352/345/348 ms |
| 资产转换 | Blender 5.2 从锁定 OIP GLB 导出 37 个 FBX 成功，校验与许可已记录 |
| Python 运行依赖审计 | pip-audit 未发现已知漏洞，3 个运行依赖 |
| 网页依赖审计 | npm audit 未发现已知漏洞 |
| Unity Editor 实际启动 | 许可检查退出，返回码 198：No valid Unity Editor license found |
| Unity WebGL 编译 | 等待用户完成 Unity Hub 账号登录和许可证激活 |
| 浏览器模型、真实动作、WSS 闭环 | 待实际 WebGL 构建后验收，不能用静态网页或纯 C# 测试代替 |

构建安装位置：`D:\UnityHub`、`D:\UnityEditors\6000.3.24f1`。Unity 安装包签名为 Unity Technologies SF，Windows 验证有效。所有安装包在本项目忽略的 `.cache/installers` 中，未提交仓库。

## 需要补完的浏览器验收

1. WebGL 加载、所有工业模型显示、浏览器控制台无错误。
2. 同一会话通过真实 EMQX：启动、暂停、手自动、复位、急停/释放；四态和灯一致。
3. 四种物料以及空载具全部完成；NG 实際搬运进入对应仓，OK 通过；无重复计数。
4. 中途暂停恢复、夹持时急停、后台标签页、控制器重启、网络中断和刷新恢复。
5. 保存构建结果、运行截图、浏览器测试报告及最终 Git 检查点。

## 回退检查点

- `c8ecf66`：已确认的设计与需求基线，已推送。
- 应用实现检查点在首次提交后记录；通过全部浏览器验收之前不标记最终完成。
