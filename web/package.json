{
  "name": "industrial-cell-webgl-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:mqtt": "node tests/mqtt-smoke.mjs"
  },
  "dependencies": { "mqtt": "5.15.2" },
  "devDependencies": { "esbuild": "0.28.2", "@playwright/test": "1.63.0" }
}
